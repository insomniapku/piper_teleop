# Pico遥控Piper机械臂 - 完整使用指南

**更新时间**: 2026-08-26  
**项目状态**: SDK已就绪，可以开始测试

---

## 📋 目录

1. [系统概述](#系统概述)
2. [环境状态](#环境状态)
3. [快速开始](#快速开始)
4. [详细步骤](#详细步骤)
5. [SDK测试](#sdk测试)
6. [控制说明](#控制说明)
7. [故障排除](#故障排除)
8. [已知问题](#已知问题)

---

## 系统概述

### 架构
```
Pico头显 ──USB──> PC <──CAN──> Piper机械臂
    │                 │
    └─ XRoboToolkit   └─ Piper SDK
         SDK              (v0.6.2)
       (v1.0.2)
```

### 控制方式
- **直接笛卡尔控制**: 使用Piper SDK的`EndPoseCtrl()`功能
- **Delta映射**: 相对运动，避免位置跳变
- **实时控制**: 100Hz控制频率
- **无需IK**: SDK内部处理逆运动学

---

## 环境状态

### ✓ 已确认可用
- **Piper SDK**: v0.6.2 已安装
- **XRoboToolkit SDK**: v1.0.2 已安装
- **CAN接口**: can0 已启动 (UP, RUNNING)
- **主控制脚本**: `pico_teleop_piper.py` 已就绪

### 📦 关键文件
```
/home/zktitan/lcz0820/
├── pico_teleop_piper.py          # 主遥操作程序
├── test_sdk_basic.py             # SDK测试脚本 (新)
├── PICO_QUICKSTART.md            # 快速开始文档
├── PICO_INTEGRATION_GUIDE.md     # 集成指南
└── piper_sdk/                    # Piper SDK源码
```

---

## 快速开始

### 前提条件检查

```bash
# 1. 检查CAN接口
ifconfig | grep can0
# 应该看到: can0: flags=193<UP,RUNNING,NOARP>

# 2. 检查Python包
pip list | grep -i "piper\|xrobo"
# 应该看到:
#   piper-sdk          0.6.2
#   xrobotoolkit-sdk   1.0.2
```

### 三步启动

```bash
# 步骤 1: 测试SDK (推荐首次运行)
python3 test_sdk_basic.py

# 步骤 2: 如果测试通过，启动遥操作
python3 pico_teleop_piper.py

# 步骤 3: 戴上Pico头显，握住右手控制器开始操作
```

---

## 详细步骤

### 1. 硬件准备

#### 1.1 Piper机械臂
```bash
# 检查机械臂是否上电
# - 电源指示灯应亮起
# - 确认急停按钮未按下

# 激活CAN接口 (如果未激活)
cd piper_sdk/piper_sdk
bash can_activate.sh can0 1000000 "1-11.2:1.0"

# 验证CAN接口
ifconfig | grep -A 1 can0
# 应该看到: UP,RUNNING,NOARP
```

#### 1.2 Pico头显
```bash
# 1. 通过USB连接Pico头显到PC
# 2. 确保XRoboToolkit PC Service正在运行
ps aux | grep XRobo

# 如果没有运行，启动PC Service:
# - Windows: 运行 XRoboToolkit PC Service.exe
# - Linux: 参考 XRoboToolkit-PC-Service README

# 3. 确认控制器已配对并开机
# 4. 戴上头显，确认跟踪正常
```

### 2. 环境检查

#### 2.1 运行SDK测试脚本
```bash
python3 test_sdk_basic.py
```

**测试内容**:
- ✓ Piper SDK导入和连接
- ✓ 机械臂使能状态
- ✓ 读取末端位置和关节状态
- ✓ XRoboToolkit SDK初始化
- ✓ 读取Pico控制器数据

**预期输出**:
```
======================================================================
  SDK基础功能测试
======================================================================

[测试 1/2] Piper SDK
----------------------------------------------------------------------
✓ Piper SDK导入成功
尝试连接机械臂...
✓ 机械臂连接成功
✓ 固件版本: X.X.X
尝试使能机械臂...
✓ 机械臂使能成功

当前机械臂状态:
  末端位置: X=150.0mm, Y=0.0mm, Z=200.0mm
  关节使能: [True, True, True, True, True, True]

[测试 2/2] XRoboToolkit SDK
----------------------------------------------------------------------
✓ XRoboToolkit SDK导入成功
初始化XR SDK...
✓ XR SDK初始化成功

请操作Pico右手控制器...
  - 握住握把
  - 按扳机
  - 移动控制器

读取数据中 (5秒)...

  [1s] 握把=0.00, 扳机=0.00, 位置=[0.123, -0.045, 0.678]
  [2s] 握把=1.00, 扳机=0.50, 位置=[0.125, -0.043, 0.680]
  ...

✓ 控制器数据正常

======================================================================
  测试总结
======================================================================

Piper SDK:         ✓ 正常
XRoboToolkit SDK:  ✓ 正常

✓ 所有SDK测试通过，可以运行遥操作程序:
  python3 pico_teleop_piper.py
```

### 3. 启动遥操作程序

```bash
python3 pico_teleop_piper.py
```

**启动流程**:
```
======================================================================
  Piper机械臂 Pico遥操作系统
======================================================================
✓ XRoboToolkit SDK已加载
✓ Piper SDK已加载
======================================================================

初始化Piper机械臂连接...
使能机械臂...
✓ 机械臂已使能
✓ 固件版本: X.X.X
配置夹爪...
✓ 夹爪已配置
移动到初始位置...
✓ 准备就绪

======================================================================
  开始遥操作
======================================================================
操作说明:
  - 握住右手控制器握把: 激活机械臂控制
  - 移动右手控制器: 机械臂末端跟随
  - 按右扳机: 关闭夹爪
  - 松开右扳机: 打开夹爪
  - B按钮: 紧急停止
  - Ctrl+C: 退出程序
======================================================================

XRoboToolkit SDK已初始化
```

### 4. 开始控制

#### 4.1 首次激活
1. **戴上Pico头显**
2. **握住右手控制器握把** (握力 > 50%)
3. 程序会显示:
   ```
   [引用设置] XR: [0.123, -0.045, 0.678], EE: [0.150, 0.000, 0.200]
   ```
4. 此时当前位置被设为参考点

#### 4.2 移动控制
- **轻轻移动控制器**，机械臂末端会跟随
- 移动映射关系:
  - 控制器向前/后 → 机械臂X轴 (前后)
  - 控制器向左/右 → 机械臂Y轴 (左右)
  - 控制器向上/下 → 机械臂Z轴 (上下)
- 默认缩放比例: `2.0` (控制器移动1cm，机械臂移动2cm)

#### 4.3 夹爪控制
- **按下扳机** → 夹爪关闭 (0mm)
- **松开扳机** → 夹爪打开 (70mm)
- 扳机值会实时映射到夹爪位置

#### 4.4 暂停和恢复
- **松开握把** → 控制暂停，机械臂保持当前位置
- **重新握住** → 重新设置参考点，继续控制

#### 4.5 停止
- **按B按钮** → 紧急停止
- **Ctrl+C** → 正常退出

---

## SDK测试

### 测试1: Piper SDK独立测试
```bash
python3 test_arm_sdk_simple.py
```

### 测试2: XRoboToolkit SDK独立测试
```bash
python3 -c "
import xrobotoolkit_sdk as xrt
xrt.init()
import time
for i in range(10):
    pose = xrt.get_right_controller_pose()
    grip = xrt.get_right_grip()
    print(f'Pose: {pose[:3]}, Grip: {grip}')
    time.sleep(0.5)
xrt.close()
"
```

### 测试3: 完整系统测试
```bash
python3 test_sdk_basic.py
```

---

## 控制说明

### 控制模式

| 操作 | 功能 | 说明 |
|------|------|------|
| **握住握把** | 激活控制 | 首次激活设置参考点 |
| **松开握把** | 暂停控制 | 机械臂保持位置 |
| **移动控制器** | 末端跟随 | Delta映射，相对运动 |
| **按扳机** | 关闭夹爪 | 扳机值0→1，夹爪70mm→0mm |
| **松开扳机** | 打开夹爪 | - |
| **B按钮** | 紧急停止 | 立即停止程序 |
| **Ctrl+C** | 正常退出 | 显示统计信息 |

### 工作空间限制

程序会自动限制机械臂在安全范围内:

```python
X轴: 100mm ~ 350mm  (前后)
Y轴: -200mm ~ 200mm (左右)
Z轴: 100mm ~ 350mm  (上下)
```

超出范围的命令会被自动裁剪。

### 可调参数

在 `pico_teleop_piper.py` 中可修改:

```python
# 第106-109行
self.position_scale = 2.0    # 位置缩放 (1.0-5.0)
                              # 值越大，控制器小动作对应机械臂大动作

self.max_delta = 0.05         # 最大单步移动 (米)
                              # 限制运动速度，防止突然跳变
```

**推荐配置**:
- 初学者: `position_scale = 1.0`, `max_delta = 0.02`
- 正常使用: `position_scale = 2.0`, `max_delta = 0.05`
- 精细操作: `position_scale = 0.5`, `max_delta = 0.01`

---

## 故障排除

### 问题1: SDK导入失败

**症状**:
```
ModuleNotFoundError: No module named 'piper_sdk'
```

**解决方案**:
```bash
# 重新安装Piper SDK
cd /home/zktitan/lqw-project/piper/piper_sdk
pip install . --force-reinstall

# 验证
python3 -c "import piper_sdk; print('OK')"
```

---

### 问题2: 机械臂连接失败

**症状**:
```
✗ 机械臂使能失败
```

**排查步骤**:
```bash
# 1. 检查CAN接口
ifconfig | grep can0
# 应该看到 UP,RUNNING

# 2. 如果CAN未启动
cd piper_sdk/piper_sdk
bash can_activate.sh can0 1000000 "1-11.2:1.0"

# 3. 检查机械臂
# - 电源是否接通
# - 急停按钮是否释放
# - CAN线是否连接牢固

# 4. 重启机械臂
# 断电 → 等待10秒 → 上电
```

---

### 问题3: Pico控制器数据全为0

**症状**:
```
⚠ 警告: 控制器位置全为0
位置=[0.000, 0.000, 0.000]
```

**排查步骤**:
```bash
# 1. 检查PC Service是否运行
ps aux | grep XRobo
# 如果没有，启动PC Service程序

# 2. 检查USB连接
lsusb | grep -i pico
# 应该能看到Pico设备

# 3. 检查控制器
# - 控制器是否开机 (LED亮)
# - 控制器是否与头显配对
# - 重启Pico头显

# 4. 重新初始化
# 关闭程序 → 重启PC Service → 重新运行程序
```

---

### 问题4: 机械臂不响应移动命令

**症状**:
- SDK命令发送成功
- 反馈值显示变化
- 但机械臂实际不动

**已知情况** (来自TELEOP_STATUS_REPORT.md):
这是之前遇到的核心问题。可能原因:

1. **控制模式未正确设置**
   ```python
   # 确保使用MOVEP或MOVEL模式
   piper.MotionCtrl_2(0x01, 0x00, 30, 0x00)  # MOVEP模式
   ```

2. **示教模式未退出**
   ```bash
   # 机械臂可能处于示教模式
   # 需要通过SDK切换到自动控制模式
   ```

3. **速度设置过低**
   ```python
   # 尝试提高速度百分比
   piper.MotionCtrl_2(0x01, 0x00, 50, 0x00)  # 50%速度
   ```

4. **物理安全锁**
   - 检查机械臂是否有物理使能开关
   - 查看用户手册中的使能流程

**当前版本改进**:
`pico_teleop_piper.py` 使用以下配置:
```python
# 第170行 - MOVEL模式，50%速度
self.piper.MotionCtrl_2(0x01, 0x02, 50, 0x00)
```

---

### 问题5: 控制卡顿或延迟

**可能原因**:
1. PC性能不足
2. USB或CAN通信延迟
3. 控制频率过高

**优化方案**:
```python
# 在pico_teleop_piper.py第248行调整
time.sleep(0.01)  # 100Hz (默认)
# 改为:
time.sleep(0.02)  # 50Hz (更稳定)
```

---

### 问题6: 机械臂移动不流畅

**调整运动参数**:
```python
# 降低缩放比例
self.position_scale = 1.0  # 更精细

# 减小最大步长
self.max_delta = 0.02  # 更平滑
```

---

## 已知问题

### 1. 机械臂历史问题 (2026-08-25报告)

根据 `TELEOP_STATUS_REPORT.md`:
- **问题**: 机械臂在CAN_CTRL模式下不响应JointCtrl命令
- **状态**: 已识别，需要进一步调查
- **当前版本**: 改用`EndPoseCtrl`（笛卡尔控制）替代`JointCtrl`

### 2. IK求解问题 (已解决)

- **旧方案**: 使用Placo IK求解器，经常失败
- **新方案**: 直接使用Piper SDK的笛卡尔控制，SDK内部处理IK
- **优势**: 更简单、更可靠

### 3. 姿态控制未实现

当前版本只控制位置(X, Y, Z)，不控制姿态(Roll, Pitch, Yaw)。

**如需姿态控制**，参考:
```python
# 在EndPoseCtrl中添加姿态参数
piper.EndPoseCtrl(
    x, y, z,
    rx, ry, rz  # 姿态 (微弧度)
)
```

---

## 安全注意事项

⚠️ **重要提示**:

1. **首次使用**
   - 使用小的`position_scale`值 (1.0)
   - 缓慢移动控制器
   - 熟悉控制感觉后再提高速度

2. **工作空间**
   - 确保机械臂周围无障碍物
   - 不要让机械臂撞击桌面或其他物体
   - 注意夹爪开合时的空间

3. **紧急情况**
   - 随时准备按B按钮紧急停止
   - 或者按Ctrl+C退出程序
   - 必要时按下机械臂急停按钮

4. **操作建议**
   - 不要突然大幅移动控制器
   - 保持平稳、连续的运动
   - 如果感觉异常，立即松开握把

---

## 技术细节

### 控制流程

```
┌─────────────────────────────────────────────────────────┐
│ 1. 读取Pico控制器位姿 (XRoboToolkit SDK)               │
│    - 位置 [x, y, z]                                     │
│    - 姿态 [qx, qy, qz, qw]                              │
│    - 握把/扳机状态                                      │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│ 2. 检查激活状态                                         │
│    - 握把 > 0.5 → 激活                                  │
│    - 首次激活 → 设置参考位置                            │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│ 3. 计算Delta                                            │
│    delta = (当前XR位置 - 参考XR位置) × position_scale  │
│    限制: |delta| ≤ max_delta                            │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│ 4. 计算目标位置                                         │
│    target = 参考机械臂位置 + delta                      │
│    工作空间裁剪: X[0.1,0.35], Y[-0.2,0.2], Z[0.1,0.35] │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│ 5. 发送控制指令 (Piper SDK)                             │
│    - MotionCtrl_2(MOVEL, 50%)                           │
│    - EndPoseCtrl(x, y, z, rx, ry, rz)                   │
│    - GripperCtrl(trigger → position)                    │
└─────────────────────────────────────────────────────────┘
                           ↓
┌─────────────────────────────────────────────────────────┐
│ 6. 重复 @ 100Hz                                         │
└─────────────────────────────────────────────────────────┘
```

### 为什么使用Delta映射？

**问题**: 如果直接将XR坐标映射到机械臂坐标:
- VR空间和机械臂空间原点不同
- 首次激活时机械臂会突然跳到新位置
- 不同的初始姿态会导致不同的行为

**解决**: Delta映射
- 只关注相对运动
- 从当前位置平滑过渡
- 一致的控制体验

### SDK对比

| 功能 | 方法1: JointCtrl | 方法2: EndPoseCtrl (当前) |
|------|-----------------|--------------------------|
| 控制空间 | 关节空间 | 笛卡尔空间 |
| IK需求 | 需要外部IK | SDK内部处理 |
| 复杂度 | 高 (需Placo) | 低 |
| 可靠性 | 有已知问题 | 稳定 |
| 直观性 | 低 | 高 |

---

## 下一步开发

### 可能的改进

1. **姿态控制**
   - 使用控制器旋转控制末端姿态
   - 添加姿态映射和限制

2. **双手控制**
   - 左手控制机械臂底座
   - 右手控制末端执行器

3. **力反馈**
   - 当接近工作空间边界时震动
   - 夹爪夹持时的触觉反馈

4. **数据记录**
   - 记录轨迹用于重放
   - 收集训练数据

5. **示教编程**
   - 记录关键点
   - 生成可执行程序

---

## 相关文档

- **快速开始**: `PICO_QUICKSTART.md`
- **集成指南**: `PICO_INTEGRATION_GUIDE.md`
- **状态报告**: `TELEOP_STATUS_REPORT.md`
- **Piper SDK**: `piper_sdk/README(ZH).MD`
- **XRoboToolkit**: https://github.com/XR-Robotics/XRoboToolkit-PC-Service

---

## 联系与支持

**项目信息**:
- 路径: `/home/zktitan/lcz0820`
- 主程序: `pico_teleop_piper.py`
- 测试脚本: `test_sdk_basic.py`

**遇到问题**:
1. 运行 `test_sdk_basic.py` 诊断
2. 查看本文档的"故障排除"部分
3. 检查 `TELEOP_STATUS_REPORT.md` 了解已知问题

---

**文档生成**: Claude Opus 5  
**最后更新**: 2026-08-26
