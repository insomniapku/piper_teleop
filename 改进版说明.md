# 改进版遥操作说明

## 🆕 新版本的主要改进

### 1. **绝对位置映射** ⭐
- **旧版本（Delta映射）**：控制器移动→机械臂相对移动
- **新版本（绝对映射）**：控制器位置→机械臂位置直接对应

### 2. **双手控制** ⭐
- 左手和右手都可以控制机械臂
- 随时切换，自动识别

### 3. **自动校准**
- 首次握住握把时，自动设置VR空间中心
- 之后在中心点周围±40cm移动

---

## 🚀 使用新版本

```bash
cd /home/zktitan/lcz0820
python3 pico_teleop_improved.py
```

---

## 📖 使用方法

### 步骤1: 启动程序
程序会初始化机械臂，等待"准备就绪"

### 步骤2: 校准（重要！）
1. 把控制器（左手或右手都可以）放在**舒适的中心位置**
2. 握住握把
3. 看到 `[校准] VR中心已设置`
4. 这个位置对应机械臂的中心工作区域

### 步骤3: 控制
- 从中心点**向前**移动 → 机械臂向前
- 从中心点**向右**移动 → 机械臂向右
- 从中心点**向上**移动 → 机械臂向上
- 移动范围：中心点±40cm

### 步骤4: 切换手
- 松开当前手的握把
- 握住另一只手的握把
- 自动切换控制

---

## 🎯 两个版本对比

### 旧版本（pico_teleop_piper.py）
```
特点: Delta映射，相对运动
优点: 安全，不会突然跳变
缺点: 不直观，需要理解参考点概念

适合: 已经熟悉的用户，需要精细控制
```

### 新版本（pico_teleop_improved.py）⭐
```
特点: 绝对映射，位置直接对应
优点: 直观，控制器在哪机械臂就在哪
缺点: 需要先校准VR中心点

适合: 新用户，需要直观控制
```

---

## ⚙️ 映射区域说明

### VR空间
```
校准中心点: 你握住握把时的位置
工作范围: 中心点 ± 40cm (立方体)
```

### 机械臂空间
```
中心点: X=22.5cm, Y=0cm, Z=22.5cm
工作范围:
  X: 10cm ~ 35cm (前后)
  Y: -20cm ~ +20cm (左右)
  Z: 10cm ~ 35cm (上下)
```

### 映射关系
```
VR中心 ← → 机械臂中心
VR向前40cm ← → 机械臂X最大值
VR向后40cm ← → 机械臂X最小值
```

---

## 💡 使用技巧

### 技巧1: 选择好的校准点
```
✓ 好的校准点:
  - 手臂自然下垂，稍微向前
  - 在你前方约30cm处
  - 高度与胸部齐平

✗ 不好的校准点:
  - 身体后方
  - 头顶上方
  - 太靠近身体
```

### 技巧2: 重新校准
如果感觉映射不对：
```
1. 松开握把
2. 把控制器移到新的中心位置
3. 重新握住
4. 系统会重新校准
```

### 技巧3: 使用左手/右手
```
左手: 适合左侧操作
右手: 适合右侧操作

可以随时切换，选择更舒适的手
```

---

## 🔧 调整映射参数

如果觉得映射范围不合适，可以修改 `pico_teleop_improved.py`：

### 第68-69行：VR工作范围
```python
self.vr_range = np.array([0.4, 0.4, 0.4])  # ±40cm
# 改为:
self.vr_range = np.array([0.3, 0.3, 0.3])  # ±30cm (更小范围)
# 或:
self.vr_range = np.array([0.5, 0.5, 0.5])  # ±50cm (更大范围)
```

### 第72行：机械臂工作中心
```python
self.robot_center = np.array([0.225, 0.0, 0.225])
# 可以调整到不同的中心位置
```

---

## 📊 实时反馈

新版本会显示详细信息：
```
[right手] 控制次数: 123 | VR偏移: [+0.123, -0.045, +0.078] | 机械臂: [0.248, -0.009, 0.235] | 扳机: 0.50
         ↑             ↑                    ↑                           ↑                    ↑
      使用的手      控制次数      从中心点的偏移距离            机械臂实际位置          夹爪开合度
```

---

## 🎮 完整工作流程

```
1. 启动程序: python3 pico_teleop_improved.py

2. 准备校准:
   - 找一个舒适的中心位置
   - 控制器放在那里

3. 校准:
   - 握住握把
   - 看到"VR中心已设置"

4. 开始控制:
   - 向前/后/左/右/上/下移动
   - 机械臂直接跟随到对应位置
   - 按扳机控制夹爪

5. 切换手（可选）:
   - 松开当前手
   - 用另一只手握住
   - 继续控制

6. 重新校准（如果需要）:
   - 松开握把
   - 移到新的中心点
   - 重新握住

7. 停止:
   - B按钮紧急停止
   - 或Ctrl+C退出
```

---

## 🆚 选择哪个版本？

### 选择新版本（pico_teleop_improved.py）如果：
- ✓ 第一次使用
- ✓ 想要直观控制
- ✓ 不需要特别精细的操作

### 选择旧版本（pico_teleop_piper.py）如果：
- ✓ 已经熟悉Delta映射
- ✓ 需要避免突然跳变
- ✓ 需要非常精细的增量控制

---

## 🚀 立即试试

```bash
python3 pico_teleop_improved.py
```

**记住**: 首次握住握把时的位置很重要，那是你的控制中心！

---

祝你控制顺利！如果还有问题，告诉我具体哪里不对。
